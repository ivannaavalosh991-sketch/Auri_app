name: auri_app
description: "Auri — asistente personal inteligente"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # -------------------------------
  # Firebase (todas compatibles)
  # -------------------------------
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.2.10

  # -------------------------------
  # UI
  # -------------------------------
  cupertino_icons: ^1.0.8
  rive: ^0.13.20

  # -------------------------------
  # Persistencia local
  # -------------------------------
  shared_preferences: ^2.3.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # -------------------------------
  # Utilidades
  # -------------------------------
  uuid: ^4.4.0
  http: ^1.2.2
  flutter_dotenv: ^5.1.0
  intl: ^0.19.0

  # -------------------------------
  # Notificaciones locales + TZ
  # -------------------------------
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4
  geolocator: ^12.0.0
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/rive/
    - .env
